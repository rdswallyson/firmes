{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && npx prisma db push --schema=packages/db/prisma/schema.prisma && npx turbo run build --filter=landing",
  "outputDirectory": "apps/landing/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
