{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && npx turbo run build --filter=landing",
  "outputDirectory": "apps/landing/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/relatorio-anual", "schedule": "0 8 1 1 *" }
  ]
}
